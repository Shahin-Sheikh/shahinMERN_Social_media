import React, { Component } from 'react';
import PropTypes from 'prop-types';

export class MovieCard extends Component {
  static propTypes = {};

  render() {
    return (
      <div>
        <h2>This is movie card page</h2>
      </div>
    );
  }
}

export default MovieCard;
